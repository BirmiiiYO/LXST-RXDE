import React from 'react'

import { Button, Container, Input } from './styles'

export const InputWithButton = () => (
  <Container>
    {' '}
    <Input />
    <Button>Send</Button>
  </Container>
)
